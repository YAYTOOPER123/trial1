{
  "private": true,
  "scripts": {
    "backend": "cd backend && .\\mvnw.cmd -q spring-boot:run",
    "frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n back,front -c auto \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
